/**
 * systems/index.js — Legacy stub, nije korišćen u novoj arhitekturi
 * Sve sisteme su u src/macro/, src/micro/, src/meta/
 */

export function updateSystems(state, input, dt) {
  // Not used — systems are in macro/micro/meta directories
}
