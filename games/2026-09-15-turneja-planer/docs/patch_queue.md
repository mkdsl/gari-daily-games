# Patch Queue — Turneja Planer

## Otvoreni patčevi
- [ ] P1 `src/systems/routing.js` + `src/main.js` — `logistics` skill `travel_cost_mult: -0.10` deklarisan u config.js ali `calcRouteCost` ne prima crew kao argument i ne čita mult → skill UI obećava popust koji ne postoji, mrtav efekat od impl-a
- [ ] P2 `src/main.js` — `city_result.budget_delta` (linija 302) = `revenue - (crew + split)`, izostavlja travel_cost oduzet ranije u `route_selected` → Results ekran prikazuje lažno veću uštedu nego što je stvarna promena budžeta
- [ ] P2 `src/main.js` — `TICKET_PRICE = 8` hardkodovano lokalno u `computeCityResults`, zaobilazi importovani `TICKET_PRICE_EUR` iz config.js → promena tiketa u config.js nema efekta na revenue kalkul

- [ ] P3 `src/content/brand_hooks.js` — Kluboslavija live-event callout: dodati `KLUBOSLAVIJA_DATES` objekt sa stvarnim datumima gradova (Štrand, Sarajevo, Guncati grand finale) i funkciju `getLiveEventCallout(cityId)` → prikazuje se na Results ekranu ako `reputation >= 6`, tekst tipa "🎪 Kluboslavija dolazi u Novi Sad — stvarni datum, stvarni šou" — converts player u potencijalnog gosta
- [ ] P3 `src/content/brand_hooks.js` + `src/content/cities.js` — Guncati ending enrichment: proširiti `getEndingBrandLines` za "Turneja Legenda" ending koji uključuje Guncati destinaciju — dodati Tom Sawyer / "povratak na selo" narativ + CTA ka Guncati masterclass programu, max 3 rečenice ispod postojećeg ending teksta
- [ ] P3 `src/share.js` + `src/content/brand_hooks.js` — Share card branding: na "Turneja Legenda" ending, `generateShareCard()` dodaje `#KluboslavijaTurneja` hashtag i MKDSLend tagline ("Zabavni radni park") u overlay tekst slike — vidljivo na screenshot-u koji ide na Stories bez ikakve akcije igrača
- [ ] P3 `src/content/brand_hooks.js` — MKDSLend meta-reveal: na svakom ending ekranu (ispod ending narrativa) dodati jedan MKDSLend red koji povlači paralelu "Ovo je šta šef zapravo radi svaki dan" + mkdslend.com placeholder URL — max 2 rečenice, stil caveman, ne advertorial
- [ ] P3 `src/content/cities.js` + `src/content/brand_hooks.js` — Prestige sezona "Fan Database" flavor: kad igrač uđe u Prestige run, `getArrivalMsg(cityId)` dobija varijantu sa "Fanovi te pamte iz prošle sezone" po gradu + Guncati-specific "Imanje te čeka ponovo" message za reputation-gate destinaciju — vezano za "povratak" brand narativ i replay motivaciju

- [ ] P2 `src/content/endings.js` + `src/render.js` — Post-run "što si propustio" moment: na svakom ending ekranu prikazati 1–2 konkretna decision card-a koji su najviše koštala budžet ili reputaciju (npr. "Sarajevo: odbio si lokalnog DJ-a → -Local Rep 2, direktna veza sa Guncati gate"). Igrač vidi tačan uzrok, ne pati od "zašto sam izgubio?" — retry motivacija raste kad fail ima ime.
- [ ] P2 `src/systems/prestige.js` + `src/render.js` — Fan DB vizualizacija po gradu pre prestige restart-a: umesto suvog "Fan Database aktivan", prikazati breakdown po gradovima ("280 fanova pamti te u Beogradu · 140 u Novom Sadu · 60 u Nišu"). Prestige tada oseća kao gradnja nečeg trajnog, ne puko ponavljanje — "još jedan run" dobi konkretan razlog.
- [ ] P2 `src/systems/crew_mood.js` + `src/content/crew_profiles.js` — Crew farewell/return dialog: na kraju run-a, jedna rečenica po aktivnom članu crew-a u zavisnosti od finalnog mood-a (Tonček pri mood ≥ 7: "Sutra opet?" / pri mood ≤ 3: "Daj mi nedelju dana, šefe"). Na prestige run-u isti dijalozi dobijaju "povratak" varijantu. Crew prestaju biti statistike — emocionalna cena gubitka raste, pull za retry jači.
- [ ] P3 `src/systems/routing.js` + `src/render.js` — "Što je moglo biti" ruta na Results ekranu: posle poslednjeg grada, jedan red sa najjeftinijom alternativnom rutom (calcRouteCost već postoji) i delta troškova vs odabrana ruta — "Direktno Niš→Sarajevo štedelo bi 120 EUR i 1 Crew Mood". Konkretan, actionable uvid koji ne kritikuje nego motiviše sledeći run.
- [ ] P3 `src/content/endings.js` — "Završeno i Plaćeno" ending upgrade: najčešći ending (budget > 500, rep 6–8) trenutno završava ravno bez emotivnog peak-a. Dodati jedan personalizovani red koji menja ton u zavisnosti od toga koliko je igrač blizu bio "Turneja Legenda" uslova — npr. "Falilo ti je 180 EUR i 1 tačka reputacije. Sledeća sezona?" vs "Solidno. Nisi legenda, ali si platih račune." Anticlimax → mini-cliffhanger.

- [ ] P3 `src/content/decisions_data.js` — city-specific decision card flavor text: svaki od 40 card-a dobija `city_flavor` polje po gradu (max 1 rečenica) — Tončekovo "sub ne vuče" zvuči drugačije u Sarajevu ("tehničar kaže da im sub ionako uvek boji" → sarkazam) nego u Guncatima ("ovo nije klub, neka ide organski" → rural zen). Mehanika se ne menja, kontekst menja ton celom eventu.
- [ ] P3 `src/content/aforizmi.js` — "noć u jednoj rečenici": posle zatvaranja svakog grada, sistem bira jedan aforizam iz pool-a od 25 linija baziranog na kombinaciji resursa (visok Reach + nizak Crew Mood = "Svi su te videli. Niko nije spavao."). Pera Period stil — telegrafski, bez moralizacije. Ne prikazuje se posle svakog decision-a (to je spam) — samo na City Result ekranu, ispod delta resursa.
- [ ] P3 `src/content/decisions_data.js` + `src/content/aforizmi.js` — Crew voice pre-event: svaki aktivni crew član ima 1–2 rečenice pre micro-odluke večeri, selektovane po gradu i crew mood-u (Tonček pri mood <= 4 u Nišu: "Ovo mi je treći Niš ove sezone. Ne pitaj me za entuzijazam." / pri mood >= 7: "Čuo sam da je klub solidan. Hajde."). Crew prestaje biti skill tag — postaje lik sa stavom prema putu.
- [ ] P3 `src/content/endings_data.js` — "Guncati Zatvoren" ending dubina: trenutno ending je jedna rečenica poraza. Dodati 3 varijante zavisno od toga ZAŠTO je reputation < 6 — (a) previše kompromisa s organizatorima (Tensions > 8): "Prilagodio si se svima. Guncati ne traži prilagodljive." (b) loša crowd quality: "Publika se pričala. Vijesti su male." (c) budžet squeezed crew: "Nisu došli glasni. Atmosfera ne laže." Isti fail, tri različite lekcije — replay motivacija raste kad gubitak ima dijagnozu.

## Završeni patčevi
- [x] P1 `src/main.js` — `budget_slider` handler: ceiling fiksovan na `available = budget - totalDailyRate(crew)` — overdraft scenario eliminisan (done 2026-09-16, commit eb689f0, potvrđeno beta_report_3 8.5/10)
